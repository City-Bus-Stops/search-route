import styled from 'styled-components';

import {
  TextWithFontWeight600,
 } from '../../styled-components';

export const Text = styled.text`
  font-size: 17px;
`;

export const StrongText = TextWithFontWeight600;
